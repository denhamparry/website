[build]
  base = ""
  command = "hugo -b $URL"
  publish = "/public"

[build.environment]
  HUGO_THEME = "hermit"

[context.deploy-preview]
  command = "hugo -b $DEPLOY_PRIME_URL"

[context.branch-deploy]
  command = "hugo -b $DEPLOY_PRIME_URL"